# Moved

This tool has been superseded by [`tools/license-admin/`](../license-admin/README.md),
which adds activation flow, grace periods, and per-seat/per-university caps on top
of the same Ed25519 keypair. This directory is kept only as a pointer and can be
deleted once you've confirmed the new CLI works for your workflow.
